# Employee Management API (FastAPI)

This project implements a simple Employee Management REST API using **FastAPI**.  
It includes CRUD operations, department filtering, average salary aggregation, and skill search.  
For simplicity, this version uses an **in-memory dictionary** instead of MongoDB.

---

## 🚀 Setup Instructions

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
